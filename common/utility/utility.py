
class FileOperation(object):
    @staticmethod
    def getFileSize(file_path):
        """Takes an absolute path of a file and returns its size in KB"""
        return 0

    @staticmethod
    def getFileName(file_path):
        """takes an absolute path of a file and returns its name"""
        return "name.txt"

    @staticmethod
    def getFileType(file_name):
        """takes a file name and returns its file type"""
        return "txt"

    @staticmethod
    def getFileDirectory(file_path):
        """takes a file path and returns its directory"""
        return "c:"